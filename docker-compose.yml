# docker-compose.yml
version: '3.8'

services:
  summarizer:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - .:/app              # ルートディレクトリ全体をマウント
      #- ./src:/app/src
      - .env:/app/.env  # 後で追加